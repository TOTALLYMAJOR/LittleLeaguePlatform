# LP-UX-002 Canonical Route Reachability

Status: corrected locally on `ux/lp-ux-002-saturday-ready`.

The removed `parent-weekly-deep-operations` disclosure mounted four broad components. This inventory separates their valid family capabilities from duplicate presentations. No disclosure or catch-all replacement is introduced.

| Previous item or region | Canonical route | Preserved context | Authorization | Proof of reachability | Disposition |
| --- | --- | --- | --- | --- | --- |
| Family Mission Control next-event pack | `/parent` | Guardian-linked child, team, next event, schedule version | `requireParentPageAccess`; guardian-scoped dashboard projection | Family Home renders `EventPassport`; child filter and Everyone summaries come from the guardian-scoped mission view | Intentionally replaced by the approved reference surface |
| Official schedule, event changes, and conflicts | `/parent/schedule?eventId={eventId}&playerId={playerId}` | Exact event and linked child where available | Parent route guard plus parent-team schedule scope | ChangeBand, Event Passport, Readiness Strip, and each per-child summary link to the canonical schedule | Reachable at an existing canonical route |
| Persisted RSVP and RSVP review | `/parent/rsvp?eventId={eventId}&playerId={playerId}` | Exact event, linked child, schedule version at write time | Parent route guard; existing RSVP access checks and authoritative write path | Event Passport, Readiness Strip, weekly event row, and per-child summary link to RSVP | Reachable at an existing canonical route |
| Family Flight Plan ride requests and accepted responsibility | `/parent/transportation?eventId={eventId}&playerId={playerId}&requestId={requestId}#transportation-request-{requestId}` | Exact request, event, linked child, direction, and schedule version | Parent route guard; existing guardian-scoped transportation projection and mutations | Transportation readiness lane deep-links to the exact request card | Reachable at an existing canonical route |
| Family Flight Plan caregiver coordination note | `/parent/transportation?eventId={eventId}&playerId={playerId}#caregiver-coordination` | Exact event and linked child are carried from Event Passport; note editor remains guardian-scoped | Parent route guard; existing family handoff service policy | Event Passport responsibility row links to the mounted `FamilyFlightPlanClient` section | Reachable at an existing canonical route |
| Official notification receipt and critical acknowledgement | `/parent/messages?teamId={teamId}&eventId={eventId}&notificationId={notificationId}#communication-message-{notificationId}` | Exact team, event, notification, and current viewer receipt | Parent route guard; team-chat route scope; receipt query keyed to signed-in parent | Critical readiness lane deep-links to the exact Communication Room message card | Reachable at an existing canonical route |
| Coach updates summary | `/parent/messages` | Guardian-authorized team scope | Parent route guard and parent-team chat scope | Family Home coach-update card and Messages navigation link | Duplicate detail presentation safely removed; canonical record retained |
| Published Parent Replay | `/parent/practice-recaps` | Guardian-linked teams and published family-visible replay records | Parent route guard and published replay projection | Family Home Replay card and topology-backed More destination | Reachable at an existing canonical route |
| Approved family-visible media | `/parent/photos` | Guardian-authorized team media only | Parent route guard and existing media visibility rules | Topology-backed More destination | Reachable at an existing canonical route |
| Family access, temporary caregivers, and season transition review | `/parent/family-access` | Signed-in parent and guardian-owned access records | Parent route guard and existing access services | Family Home access card and primary Family navigation | Reachable at an existing canonical route |
| Family preferences | `/parent/settings` | Signed-in parent preference records | Parent route guard and parent dashboard scope | Topology-backed More destination and command navigation | Reachable at an existing canonical route |
| Account, membership, security, and sign out | `/account` | Authenticated account and active role | Signed-in route guard and active-role authority | Topology-backed More destination | Reachable at an existing canonical route |
| General ParentDashboard schedule, media, updates, and readiness summaries | Routes above | Each record is owned by its specialized canonical route | Route-specific guards and scoped adapters | Family navigation, More, Event Passport, Readiness Strip, and Family Home cards | Duplicate catch-all presentation safely removed |

Snack, volunteer, balance, equipment-exchange, and support demonstrations inside the legacy broad `ParentDashboardClient` were not authoritative destinations in the removed disclosure and are not represented as retained LP-UX-002 capabilities. This correction does not create links to demonstrations or placeholders.

Focused proof: `components/family/canonical-reachability.test.ts`, `lib/navigation/route-topology.test.ts`, `components/parent-weekly-dashboard.test.tsx`, and browser deep-link evidence in the LP-UX-002 proof manifest.
