# Capability Matrix

This matrix separates current scaffold capability from production-ready capability. The root Next.js app now has Supabase-backed slices for registration, approval, memberships, Team Portal reads/theme writes, Team Chat persistence, and provider/mobile foundations. Some UI surfaces still use typed seed data until their screens are wired to the new adapters.

| Capability | Current routes/surfaces | Current state | Production gap |
| --- | --- | --- | --- |
| Multi-theme system | `/team-portal`, `/admin/themes`, `/api/admin/theme-defaults`, `/api/admin/team-logos` | Theme presets for soccer, football, baseball, scouts, golf, tennis, swim, and generic. Supabase teams store `themeKey`, mascot, and colors. Organization tenant defaults store default theme, colors, mascot, and logo status for future teams. The admin customization workbench now groups identity/colors, tenant defaults, logo asset metadata review, and 20-surface launch proof. Brand-profile validation models logo, banner, display name, short name, fallback avatar, primary/secondary/accent/button colors, hero copy, and published status for 20 target surfaces. PWA manifest, service-worker, and brand/icon asset revisions are explicitly versioned with `npm run qa:pwa-cache-proof` to avoid stale app branding after deploys. | Hosted provider proof for binary upload, public logo rendering, email rendering, push identity, and approved real-logo cache proof. |
| Theme designer | `/team-portal`, `/admin/themes` | Assigned coach/admin theme writes save to Supabase with authenticated session-derived actor IDs and audit events. Admin console shows all teams, logo/default status, last audit evidence, mobile/dark previews, contrast checks, launch validation for all 20 target brand surfaces, success metrics, monitoring events, alert rules, coach feedback questions, and a session-bound logo metadata queue for HTTPS asset review. `npm run qa:contrast-proof` now checks major parent, coach, admin, and team routes across light, dark, and team-theme surfaces with failure screenshots. | Browser QA with several real approved test brands, production monitoring dashboards/alerts, and logo review workflow proof. |
| Parent dashboard | `/parent`, `/parent/rsvp` | Parent-scoped child/team schedule, coach update, RSVP needs, recent media, RSVP button payloads, persisted RSVP change timelines, snack/volunteer openings, and notification preferences load from Supabase for the signed-in user when active guardian/player rows exist. QA parent proof exists via `npm run supabase:qa-users` and `npm run qa:session-proof`, including signed-out access gates plus browser-level RSVP, schedule preference, snack, and volunteer actions verified against Supabase QA rows. The passing manual GitHub `Supabase QA proof` run from 2026-06-28 remains preserved evidence for the earlier route proof. Route-level tests verify RSVP, preference, unsubscribe, snack, and volunteer writes use the verified Supabase session user, and component tests cover support-friendly RSVP timeline rendering. | Hosted proof for RSVP timeline rendering after migrations are applied and production observability for no-response support patterns. |
| Coach dashboard | `/coach`, `/coach/rsvps`, `/coach/parent-replay`, `/api/coach/weekly-update` | Assigned-team RSVP summaries, scoped RSVP change timelines, RSVP reliability patterns, weekly update drafts, weather, snacks, volunteers, and persisted action payloads load from Supabase for the signed-in user when active coach membership rows exist. Weekly updates save as announcements plus pending `team_broadcast` email drafts without provider sends. `/coach/parent-replay` and `/coach/rsvps` now use the same Supabase-scoped coach state for assigned-team reads. QA coach proof exists via `npm run supabase:qa-users`, `npm run qa:session-proof`, and `npm run qa:ai-coach-proof`, including hosted 2026-07-01 Parent Replay publish proof and hosted 2026-07-02 AI provider rewrite proof. Signed-out and missing-coach states show explicit access gates. Route-level tests verify snack, volunteer, weather, weekly-update, provider-review, retry-plan, Parent Replay, and AI workspace actions use the verified Supabase session user. | Browser-level weekly-update proof, hosted RSVP timeline proof, and richer approval queue UI. |
| Admin dashboard | `/admin`, `/admin/health`, `/admin/imports`, `/admin/invites`, `/admin/operations`, `/admin/observability`, `/admin/security` | Operations views summarize teams, registration queue, sponsors, notifications, readiness, imports, invites, provider inventory, approval queues, RLS/security proof, and recent audit logs. `/admin/observability` aggregates auth failures, RLS denials, provider retries, webhook failures, public-intake throttles, admin actions, and media/chat moderation from Supabase audit, delivery-attempt, webhook, public-limiter, and moderation rows first, with external dashboard hooks left env-gated. New `components/features/AdminAuditPanel` renders admin audit/health/readiness rows through domain policies only. | Hosted production audit/observability proof, external alert-hook configuration if desired, and browser-level admin action proof with signed-in admin session artifacts. |
| Team management | `/team-portal`, `/admin`, `/admin/teams` | Team selector, roster summary, branding, theme presets, team feature hub, team CRUD, division/season setup, coach assignment, roster lifecycle, tenant isolation, and automatic team-builder previews with sibling/friend/skill constraints plus admin-only team-build plan persistence. | Browser-level admin publish proof, explicit age-band/player-evaluation fields, and hosted team-build plan smoke tests. |
| Registration system | `/registration`, `/admin`, `/admin/registrations`, `/api/registration-requests` | Parent self-registration creates pending Supabase review requests with local fallback. Public intake is unauthenticated but durable-rate-limited through Supabase-backed buckets with `429`, `Retry-After`, and `X-RateLimit-*` headers plus memory fallback if the shared store is unavailable. Admin approve/reject routes create audited approval actions, player/guardian/invite records where appropriate, and derive reviewer identity from the verified session. | Browser-level live approval tests and broader guardian verification policy. |
| Schedule management | `/schedule`, `/team-portal`, `/coach`, `/api/schedule` | Event edits and new weekly event series persist through the authenticated Supabase schedule service with coach/admin checks, active-season locks, managed venue records, team/venue conflict detection, recurring event expansion, event change logs, field reservations, and pending provider-safe notification drafts. | Hosted browser proof for create/update/cancel/recurrence and richer provider delivery workflow. |
| Weather alerts | `/coach`, `/team-portal`, `/api/weather-alerts/draft`, `/api/provider-delivery/review`, `lib/services/weather` | Weather draft creation now uses the provider-order service: National Weather Service first, Open-Meteo fallback, and optional Tomorrow.io. Every provider result normalizes into a draft `WeatherEventDraft` before the Supabase weather-alert row is saved. Signed-in Coach dashboard reads scoped weather rows and has an authenticated draft trigger. Provider delivery review can approve/reject notification drafts and write delivery-attempt logs without sending externally. | Run hosted provider credential proof and browser-level live-data action proof. |
| Push notifications architecture | `/admin`, `/admin/operations`, `/admin/observability`, `/schedule`, `/parent`, `/api/push-subscriptions`, `/api/notification-preferences`, `/api/notification-preferences/unsubscribe`, `/api/mobile-usage-events`, `/api/provider-delivery/review`, `/api/provider-delivery/batch-review`, `/api/provider-delivery/retry-plan`, `/api/internal/notification-send-worker`, `/api/provider-webhooks/sendgrid`, `/api/provider-webhooks/twilio` | Notification records model channels/status; PWA service worker exists; Web Push subscriptions can be stored; parent schedule-alert preferences and unsubscribes save to Supabase by verified session user. Provider review checks provider/channel match, recipient preferences, provider credential readiness, and idempotency before writing queued or suppressed delivery-attempt records. Admin operations now provides filterable provider delivery triage with batch approve/reject, suppression reasons, retry context, and attempt history. A secret-gated worker claims due attempts, re-checks preferences/quiet hours/urgent-only SMS rules, and sends through env-gated SendGrid, Twilio Messaging Service, or Web Push adapters when configured. SendGrid and Twilio webhooks are signature-gated, deduped, and reconcile delivery/open/bounce/failure status without treating provider acceptance as delivery. `/admin/observability` surfaces retry/dead-letter and webhook-failure counts from existing rows. Hosted 2026-07-01 browser proof covers provider-delivery review rows; hosted credential/send proof remains pending. | Hosted credential proof and external alert hook configuration if desired. |
| Google Maps integration | `/team-portal`, `/schedule`, `/team-chat`, `/api/field-locations` | Managed field-location records store name, address, coordinates, Google Place ID, approved map fallback URL, approved embed URL, field label, notes, and active/inactive status. `/schedule` lets staff save venue metadata through the authenticated schedule path; `/api/field-locations` is session-gated and org-admin checked for standalone venue CRUD. Team Portal uses an iframe only when approved managed embed metadata exists, otherwise it shows a directions fallback and explicit no-route-tracking copy. Venue markers use managed field labels and coordinates when available. | Hosted proof with real Google Maps browser key restrictions, quota monitoring, and richer field-layout authoring screens. |
| Team photos/media | `/parent`, `/team-portal`, `/admin`, `/api/media/moderation`, `/api/media/report`, `/api/media/uploads/intent`, `/api/media/uploads/finalize` | Supabase media reads filter parent/team surfaces to approved media. Families can report media. Admins/coaches can hide, restore, remove, and set team/org visibility through authenticated APIs with audit events. Authenticated team members/admins can create Supabase Storage signed-upload intents and finalize uploaded JPEG/PNG/WebP/MP4 rows. Object paths are organization/team scoped, uploaded rows stay `pending` until moderation, and storage metadata records upload status, scan status, retention policy, and takedown fields. | Hosted bucket proof, automated scanning provider proof, and richer provider embed previews. |
| Snack schedules | `/parent`, `/coach`, `/team-portal`, `/api/snack-slots/claim`, `/api/snack-slots/unclaim`, `/api/snack-slots/reminders/draft` | Signed-in parent and coach dashboards read scoped Supabase snack slots when linked rows exist; claim and unclaim buttons call authenticated Supabase endpoints. Claims enforce slot caps, unclaims are limited to the assignee or staff, reminder drafts create pending `snack_reminder` notifications only, and every write records audit evidence. Hosted 2026-07-01 QA browser proof verifies the parent snack claim against Supabase rows. | Hosted proof for unclaim/reminder drafts and production observability for reminder queues. |
| Volunteer signups | `/parent`, `/coach`, `/team-portal`, `/api/volunteer-signups/claim`, `/api/volunteer-signups/unclaim`, `/api/volunteer-signups/reminders/draft` | Signed-in parent and coach dashboards read scoped Supabase volunteer roles when linked rows exist; claim and unclaim buttons call authenticated Supabase endpoints. Claims enforce role caps, unclaims are limited to the assignee or staff, reminder drafts create pending `volunteer_reminder` notifications only, and every write records audit evidence. Hosted 2026-07-01 QA browser proof verifies the parent volunteer claim against Supabase rows. | Hosted proof for unclaim/reminder drafts, richer approval policy configuration, and production observability for reminder queues. |
| Sponsor management | `/admin`, `/api/admin/sponsors` | Admin sponsor CRUD saves to Supabase with authenticated session-derived actor IDs, audit events, pending/active/expired status, placement settings, queued logo asset rows, Stripe Product/Price lookup proof, invoice references, payment-proof status, and admin-only sponsor billing record persistence. The admin UI keeps sponsor billing separate from child-facing sponsor display. | Live Stripe collection only if sponsors pay through the platform, webhook signature proof, richer asset review queue, and public display QA. |
| Mobile-first UI | All routes | Responsive CSS stacks grids/navigation for small viewports. PWA/native usage metrics are recorded so Expo stays evidence-gated and can reuse shared domain models if justified. | Mobile QA pass, touch targets, offline states, and app-store requirements if Expo is added. |
| PWA installation | All routes, `/offline`, `public/manifest.webmanifest`, `public/sw.js`, `/api/mobile-usage-events` | Manifest, service worker, install prompt UX, standalone build asset copy, explicit offline fallback, install/standalone usage measurement, versioned shell/runtime caches, network-first navigation refresh, and revisioned manifest/icon branding assets exist. Anonymous mobile usage intake is durable-rate-limited with the same public limiter contract as registration. Smoke tests assert `/offline`, service-worker fallback, usage recorder wiring, and PWA cache/branding invalidation through `npm run qa:pwa-cache-proof`. | Richer install QA across Chrome/Edge mobile and desktop plus hosted stale-brand proof after a real approved brand asset rotation. |
| Dark mode | All routes | System `prefers-color-scheme: dark` CSS variables are present, and `npm run qa:contrast-proof` runs route-level contrast checks across major parent, coach, admin, and team surfaces in light, dark, and team-theme modes. | Hosted contrast proof after deployments, manual toggle if desired, and deeper per-team brand contrast sampling with real approved test brands. |
| Branded Team Chat | `/team-chat`, `/api/team-chat/*` | Branded clubhouse UI now loads Supabase chat data, creates missing channels, saves messages/announcements, writes moderation/read receipts, subscribes to Realtime changes, and requires authenticated sessions for writes. Team members can report visible messages, coaches/admins can review report rows, and the retention runner invokes the existing purge function with an audit row. | Hosted browser proof for report/review/retention, scheduled retention automation, push alerts, and deeper policy screens. |
| Parent Replay | `/coach/parent-replay`, `/team-portal`, `/api/coach/parent-replay` | Coach selects 2-3 focus areas; deterministic generator creates home activities, translations, memory moment, video recommendation, parent tip, skill cards, and quest. Rookie Coach Assist adds local Chaos Button reset copy, Coach Voice Coach wording, team-energy drill adaptation, and Parent Reinforcement Loop drafts before any coach-approved replay is queued. Authenticated assigned coaches/admins can publish the replay to Supabase with reviewed/published evidence and pending parent notification drafts. Hosted 2026-07-01 QA browser proof verifies Parent Replay publish rows and pending provider-review boundary rows. Team Portal reads persisted replay history for families. | AI/provider boundary if generated plans become AI-backed, richer template authoring UI, browser proof for the sideline reset interaction, and completion tracking. |
| AI-generated learning plans | `/team-portal`, `/coach/parent-replay`, `/api/coach/ai-workspace` | Parent Replay remains deterministic local guidance. AI Coach Workspace can request OpenAI Responses API rewrites through an authenticated server route for assigned coaches/admins only when provider env is configured. Requests use `store: false`, local privacy filters, source evidence, approved-media filtering, and review-only output. Hosted proof passed with `QA_PROOF_BASE_URL=https://www.leaguepilot.us npm run qa:ai-coach-proof`, capturing `output/playwright/ai-coach-provider-rewrite-qa-session-live.png`. Safety evals cover cross-team data, private contacts, hidden media/messages, unsupported provider-send/publish claims, and unsourced private/external claims. | Preview env configuration remains out of launch scope until a real preview branch is chosen; continued approval/audit gates remain required before any family-facing use. |
| Assistive suggestions | `/admin`, `/coach`, `/parent` | Deterministic admin, coach, and parent suggestion cards summarize existing scoped records. They recommend, draft, and prioritize only; sensitive actions still require human clicks and existing private APIs. | Optional AI provider expansion only after evals for privacy, team scope, provider-send safety, and hallucinated records. |
| Reporting and archive | `/api/admin/exports`, `docs/archive-readiness-checklist.md` | Active org admins can generate audited CSV exports for roster, contacts, schedule, RSVP, snacks, volunteers, sponsors, and notifications. Archive checklist requires non-chat season preservation and chat-retention deletion proof. | Hosted archive smoke proof after migrations are applied and real season close workflow is run. |

## State Legend

- Local scaffold: works in the current app with typed seed data and reducer state only.
- Provider boundary: record/draft exists, but no external send or provider call occurs.
- Production gap: needed before real families, teams, money, or provider delivery depend on the capability.
