# Capability Matrix

This matrix separates current scaffold capability from production-ready capability. The root Next.js app now has Supabase-backed slices for registration, approval, memberships, Team Portal reads/theme writes, Team Chat persistence, and provider/mobile foundations. Some UI surfaces still use typed seed data until their screens are wired to the new adapters.

## 2026-07-20 Coordination Loops and Operational-Truth Hardening

| Capability | Local implementation | Required proof before production claim |
| --- | --- | --- |
| Five closed coordination loops | Provable message receipt lanes and recipient acknowledgment; Season Launch Wizard with traced admin roster commit/rollback; coach Plan → Practice → Parent Replay receipts; guardian-scoped multi-child Family Flight Plan and caregiver handoffs; coach/admin Game-Day Resolution Room. Migration `0024` supplies role policies plus atomic high-impact RPCs. | Apply migration `0024` to the intended QA environment, run real-session RLS and signed-in role browser proof, prove provider webhooks in sandbox before delivery claims, then deploy and repeat hosted proof. The empty-database migration and transactional RPC smoke pass locally is implementation proof only. |
| Verified context and evidence lanes | `ActiveContext`, action-derived `PermissionSet`, independent record/approval/publication/delivery/acknowledgment/freshness lanes, conservative rollups, context-keyed shell/cache reset. | Apply migration `0023`; run RLS and direct cross-role browser proof in the intended environment. |
| RSVP/attendance concurrency and offline | Versioned/idempotent RSVP and attendance RPCs, explicit `409` reconciliation, Field Mode, private context outbox limited to RSVP/attendance/notes. | Both environment and organization offline gates, RLS proof, two-guardian concurrency proof, reconnect/conflict browser journeys. |
| Explainable admin operations | Deterministic priority scores/reasons, dense blockers, split operational context, server-recomputed season archive preview with reason/hash/count/audit. | Signed-in admin browser proof and audit readback; `IMPACT_PREVIEW_SECRET` configured server-side. |
| Provider delivery lifecycle | SendGrid, Twilio Messaging Service, and VAPID adapters; approval-only worker; verified replay-safe webhooks; acceptance/delivery/read/acknowledgment separate. | QA allowlist sandbox evidence, consent/suppression proof, SMS registration/opt-out/cost controls, hosted configuration, operational monitoring. Default disabled. |
| Parent Replay and AI trust | Coach practice-run plans, starts, completions, and observations persist as separate receipts. A completed same-team receipt may be source-manifested into one Parent Replay; draft/approve/publish remain separate and publication creates notification drafts only. Structured Outputs use `store: false`, local privacy filters, refusal closure, and source hashes/manifests. | Migration/RLS and refreshed hosted coach/family proof. AI never has publish/send/access/tool authority. |
| Volunteer marketplace | Compare-and-set claims, idempotent receipts, waitlist, withdrawal, staff-reviewed transfer/promotion, no parent contact exposure. | Migration/RLS plus concurrent duplicate-claim browser/API proof. |
| Private media | Tenant/team private quarantine, signed upload initiation, magic-byte/decode/hash/size checks, EXIF-stripping re-encode, scan evidence, consent, family release, retention proof. | Production scanner, storage/RLS, consent/review, deletion and hosted browser proof. Upload/release default disabled. |
| Family Balance and Stripe | Proof-safe obligation labels; Connect Standard onboarding; direct connected-account Checkout Sessions; replay-safe signed webhooks; browser return never confirms. | Stripe test-mode webhook/RLS/hosted proof and organization/environment gates. Payment collection default disabled. |
| Prompt workflow companion | Side-effect-free `codex_spec` and `codex_debug` exports, four system profiles, CLI commands, VS Code tasks, dated evolution timeline. | Local unit/CLI proof is sufficient because it prints prompts only and performs no provider or repo mutations. |

| Capability | Current routes/surfaces | Current state | Production gap |
| --- | --- | --- | --- |
| Public family discovery | `/`, `/schedule`, `/registration`, `/auth` | `Request Team Access` is the primary signed-out action and Sign In is secondary. Public schedule uses a responsive agenda and event-detail composition with team, activity, date, honest unpublished-arrival state, opponent, venue, field, official status, directions, and Apple/Google/Outlook/download calendar actions. Registration and sign-in fields begin empty; access copy explains league review, timing, privacy, and next steps. Public schedule and team choices are restricted to one canonical organization and current teams. The install prompt remains ineligible until a signed-in RSVP confirmation or critical-message acknowledgment. Parent Replay includes a concrete, coach-approved, child-private family example. Local browser proof passes at 320, 390, 768, and 1440 pixels with no document overflow or undersized primary-route controls. | Configure `PUBLIC_ORGANIZATION_ID` plus the review target in each hosted environment, deploy, repeat browser proof against the production alias, and collect moderated comprehension/assistive-technology evidence. |
| Family access activation | `/access/status`, `/invite/recover`, `/invite/expired` | Reference-plus-email status checks reveal only the existing request state, masked child match, requested team, and safe next step. Recovery is blank by default, rate-limited, enumeration-safe, scoped to the canonical organization when configured, and records a provider-free admin-review audit for a matched invitation. Neither route changes invitation, guardian, membership, or provider truth. | Implement and prove signed single-use invitation preview/acceptance, identity mismatch and revocation paths, first-sign-in setup, additional guardian approval, hosted abuse controls, and real-session RLS. |
| First-sign-in preferences | `/parent/setup`, `/api/parent/setup`, `/api/auth/session-landing` | Migration `0025` adds language, translation, shared-device preview, and completion fields to the adult profile. A service-only atomic RPC requires existing active parent-team access, saves critical and routine preferences plus quiet hours/timezone, and records the signed-in adult’s action. Translation is labeled non-authoritative; channel choice is not called verified or delivered. No provider call occurs. Session landing fails open to the existing parent home when the migration is unavailable, avoiding an onboarding trap. | Apply `0025` to QA, run real-session RLS and populated signed-in mobile/desktop proof, then separately verify email/push/SMS consent, ownership, suppression, and provider delivery before channel-ready claims. |
| Parent invitation acceptance | `/invite/accept`, `/api/invites/preview`, `/api/invites/accept` | Migration `0026` adds attributed one-time acceptance. A verified secret reveals only masked approved scope; acceptance requires a verified session whose profile email matches the invitation, a pending unexpired invitation, active season, and existing invited guardian row. One transaction activates only that guardian/team scope, marks the invitation accepted, and writes audit. Return-to sign-in rejects protocol-relative paths. | Apply `0026` in QA and prove valid, wrong-account, expired, revoked, already-accepted, replay, and cross-tenant cases. Refactor approved invite creation to securely issue a deliverable one-time secret through the human-approved provider boundary; current stored hashes alone cannot reconstruct a link. |
| Route topology and role IA | Canonical `/parent/*`, `/coach/*`, `/admin/*`; compatibility `/schedule`, `/team-chat`, `/team-portal`, `/coach/rsvps`, `/coach/parent-replay`, `/admin/themes`, `/admin/security`, `/admin/archive`, `/admin/guardian-links` | A pure route topology registry drives AppShell sidebar, mobile nav, command search, canonicalHref metadata, compatibility hiding, prototype noindex, and role-home switch links. A server-only ShellAccess helper derives parent, coach, and admin capabilities from the Supabase server session; role wrappers scope schedule/chat/portal data before render. The primary parent, coach, and admin homes now use the Season Certainty information architecture on the first viewport. | Hosted browser route-topology proof and compatibility-route telemetry remain optional follow-up work; compatibility routes stay available for old links. |
| Next-level command center | `/`, `/parent`, `/coach`, `/coach/practice-recaps`, `/admin` | A pure `lib/domain/next-level.ts` read model ranks the top 12 upgrade modules and feeds a reusable role-aware command center. The UI shows today actions, onboarding, admin review queues, registration review, coach practice planning, drill-video collections, parent recap timeline, notification readiness, provider-review boundaries, PWA install posture, push preference coverage, and schedule conflicts. | Hosted browser/mobile screenshot proof for every role after this slice, plus deeper persistence only where existing Supabase routes do not already cover the underlying workflow. |
| Multi-theme system | `/team-portal`, `/admin/themes`, `/api/admin/theme-defaults`, `/api/admin/team-logos` | Theme presets for soccer, football, baseball, scouts, golf, tennis, swim, and generic. Supabase teams store `themeKey`, mascot, and colors. Organization tenant defaults store default theme, colors, mascot, and logo status for future teams. The admin customization workbench now groups identity/colors, tenant defaults, logo asset metadata review, 20-surface launch proof, interactive mascot/mobile/Game Day element toggles, and local mascot artwork preview. Brand-profile validation models logo, banner, display name, short name, fallback avatar, primary/secondary/accent/button colors, hero copy, and published status for 20 target surfaces. | Hosted provider proof for durable binary upload, public logo rendering, email rendering, push identity, and web cache invalidation. Local mascot preview is not persisted storage. |
| Theme designer | `/team-portal`, `/admin/themes` | Assigned coach/admin theme writes save to Supabase with authenticated session-derived actor IDs and audit events. Admin console shows all teams, logo/default status, last audit evidence, mobile/dark previews, interactive element visibility controls, contrast checks, launch validation for all 20 target brand surfaces, success metrics, monitoring events, alert rules, coach feedback questions, local mascot upload preview, and a session-bound logo metadata queue for HTTPS asset review. | Browser QA with several real approved test brands, production monitoring dashboards/alerts, and logo review workflow proof. |
| Parent dashboard | `/parent`, `/parent/schedule`, `/parent/messages`, `/parent/photos`, `/parent/practice-recaps`, `/parent/family-access`, `/parent/settings`, `/parent/rsvp` | Parent-scoped child/team schedule, coach update, RSVP needs, recent media, snack/volunteer openings, and notification preferences load from Supabase for the signed-in guardian. Family Flight Plan combines all linked children’s upcoming time, location, RSVP, weather, gear, obligations, and overlaps. Guardian-confirmed caregiver handoffs are event/player scoped, audited, cancelable, and do not create identity or access. Communication Room separates Critical, Updates, and Conversation, withholds unapproved wording, keeps multi-child context visible, and shows publish, delivery, read, and acknowledgment evidence independently. Isolated QA now proves two linked children across two teams, archived-team exclusion, reply persistence, recipient acknowledgment, attributed audit history, and zero provider sends. | Promote migrations only after explicit production approval; prove handoff save/cancel, overlaps, offline/reconnect conflicts, and the full parent route set in signed-in hosted mobile/desktop sessions. |
| Coach dashboard | `/coach`, `/coach/attendance`, `/coach/practice-recaps`, `/coach/schedule`, `/coach/messages`, `/coach/weather-fields`, `/coach/drafts`, `/coach/roster`, `/coach/snacks-volunteers`, `/coach/settings`, `/coach/rsvps`, `/coach/parent-replay`, `/api/coach/weekly-update` | Assigned-team readiness remains server-scoped. Practice Recaps persists reviewed plans, explicit starts, required completion observations, and single-use links into Parent Replay source evidence. A local water-break timer derives remaining time from a target timestamp so suspended tabs recover without silent drift. Injury contacts load only after service-level assigned-team authorization; numbers stay concealed until coach reveal, medical-decision evidence stays separate, and `tel:` links open the device dialer without automated provider calls. Coach Schedule adds the reviewed Resolution Room. | Apply migration `0024`; run signed-in hosted coach proof with populated guardian/emergency-contact rows, then prove plan/start/complete/replay and resolution audit readback. |
| Admin dashboard | `/admin`, `/admin/family-access`, `/admin/branding`, `/admin/security-audit`, `/admin/reports-archive`, `/admin/media-review`, `/admin/safety-weather`, `/admin/communications`, `/admin/schedule-venues`, `/admin/message-delivery-review`, `/admin/sponsors`, `/admin/settings`, `/admin/health`, `/admin/imports`, `/admin/invites`, `/admin/operations`, `/admin/security` | Admin operations remain organization-scoped. `/admin/imports` is now a Season Launch Wizard with readiness gates, staged row evidence, warning confirmation, atomic provenance commit, and downstream-safe rollback. Message Delivery Review renders independent draft/approval/acceptance/delivery/read/acknowledgment lanes and invokes approval only; worker/provider execution remains separate. Admin Schedule includes the same reviewed Resolution Room with atomic audit/change/notification-draft evidence. | Apply migration `0024`; signed-in hosted admin proof for launch commit/rollback, provider sandbox receipts, and game-day decisions. Provider presence alone is not send/delivery proof. |
| Team management | `/team-portal`, `/admin`, `/admin/teams` | Team selector, roster summary, branding, theme presets, team feature hub, team CRUD, division/season setup, coach assignment, roster lifecycle, tenant isolation, and automatic team-builder previews with sibling/friend/skill constraints plus admin-only team-build plan persistence. `/admin/teams` now starts with a tenant setup guide, reset actions for new season/team/player records, and clearer empty-state blocking copy before families are invited. | Browser-level admin publish proof, explicit age-band/player-evaluation fields, hosted team-build plan smoke tests, and deeper first-tenant setup proof. |
| Registration system | `/registration`, `/admin`, `/admin/registrations`, `/admin/guardian-links` | A family submits a blank `Request Team Access` form against the canonical organization’s current teams and receives pending review truth with a reference. The public surface explains verification timing, privacy, and next steps without exposing the admin queue. Approval and guardian-link repair require verified organization-admin authority, an existing parent profile, bounded verification evidence, and audited player/guardian/membership changes. | Hosted/browser approval and repair proof, plus stronger identity-verification evidence beyond an admin-reviewed correlation signal. |
| Schedule management | `/schedule`, `/parent/schedule`, `/coach/schedule`, `/admin/schedule-venues`, `/api/game-day-resolution` | Schedule CRUD, conflict checks, venue records, calendar exports, and role-scoped reads are implemented. The Game-Day Resolution Room combines event/weather/RSVP/roster evidence; an atomic staff-reviewed decision records monitor/confirm/delay/cancel, applies delay/cancel truth, and creates pending family notification drafts without provider execution. | Apply migration `0024`, prove coach/admin RLS and concurrent/idempotent resolution behavior in QA, and refresh hosted browser proof. |
| Weather alerts | `/coach`, `/team-portal`, `/api/weather-alerts/draft`, `/api/provider-delivery/review`, `lib/services/weather` | Weather draft creation now uses the provider-order service: National Weather Service first, Open-Meteo fallback, and optional Tomorrow.io. Every provider result normalizes into a draft `WeatherEventDraft` before the Supabase weather-alert row is saved. Signed-in Coach dashboard reads scoped weather rows and has an authenticated draft trigger. Provider delivery review can approve/reject notification drafts and write delivery-attempt logs without sending externally. | Run hosted provider credential proof and browser-level live-data action proof. |
| Push notifications architecture | `/admin/message-delivery-review`, `/parent`, provider review/worker/webhooks, `/api/notifications/acknowledge` | Notification records preserve draft/approval/queued states. Gated SendGrid, Twilio, and VAPID adapters execute only approved attempts; replay-safe verified webhooks record provider acceptance, delivery/failure, and read separately. The UI renders those lanes without inference, and recipient acknowledgment is an atomic, recipient-scoped action requiring an existing attempt. | Provider sandbox allowlist, hosted credentials, consent/suppression and operational monitoring proof. Adapters are disabled by default and presence is not delivery proof. |
| Google Maps integration | `/team-portal`, `/team-chat`, `/api/field-locations` | Map links exist and field-location API stores Google Maps URLs/embed URLs when key is configured. | Embedded map UI, venue marker management, quota handling, and field layout metadata screens. |
| Team photos/media | Parent/team/admin reads, moderation/report, private upload and family-release routes | Family reads require approved moderation, clean scan evidence, and family-release approval. Uploads remain private and quarantined through validation, EXIF-stripping re-encode, consent, scan, review, retention, and deletion evidence. | Apply migration/RLS; configure and prove the production scanning adapter and private storage paths. Upload/release remains disabled until then. |
| Coach drill video references | `/coach/practice-recaps`, `/admin/media-review`, `/api/coach/drill-videos`, `/api/coach/drill-video-assignments`, `/api/admin/drill-videos/review`, `/api/admin/drill-video-sources/review` | Coaches can paste YouTube drill URLs for server-side Data API metadata validation. Supabase stores references, source-channel allowlist state, Made-for-Kids and embeddability metadata, admin review state, and coach-only assignments to teams/practice events. Approved videos embed through `youtube-nocookie.com`; no download, rehost, thumbnail proxy, clip extraction, parent-facing display, or athlete-specific assignment is connected. | Hosted proof with a configured `YOUTUBE_DATA_API_KEY`, in-app YouTube search, playlist import, Vimeo/native/licensed provider support, parent-facing release review, and athlete-specific assignment privacy rules. |
| Snack schedules | `/parent`, `/coach`, `/team-portal`, `/api/snack-slots/claim` | Signed-in parent and coach dashboards read scoped Supabase snack slots when linked rows exist; claim buttons call the authenticated Supabase claim endpoint. Route-level action tests assert the verified session user is used. Hosted 2026-07-01 QA browser proof verifies the parent snack claim against Supabase rows. | Reminders, conflicts, audit, and notification integration. |
| Volunteer signups | Parent/coach/team portal plus claim/waitlist/transfer/promotion APIs | Actor is session-derived; compare-and-set and idempotency prevent double claim. Families may waitlist or withdraw; transfers and promotion remain staff-reviewed without exposing contact details. | Migration/RLS and duplicate-claim/concurrent-promotion browser proof. |
| Sponsor management | `/admin`, `/api/admin/sponsors` | Admin sponsor CRUD saves to Supabase with authenticated session-derived actor IDs, audit events, pending/active/expired status, placement settings, queued logo asset rows, Stripe Product/Price lookup proof, invoice references, payment-proof status, and admin-only sponsor billing record persistence. The admin UI keeps sponsor billing separate from child-facing sponsor display. | Live Stripe collection only if sponsors pay through the platform, webhook signature proof, richer asset review queue, and public display QA. |
| Money + sponsors community commerce | `/parent`, `/team-portal`, `/admin/sponsors`, `/api/parent/family-wallet`, `/api/admin/revenue-summary` | Parent Family Wallet, admin League Revenue Dashboard, Community Sponsor Matchmaker, and Local Business Team Pages are implemented as proof-safe read models over existing guardian/team/sponsor data. Parent wallet reads are scoped to the authenticated guardian session. Admin revenue reads require active organization-admin access. Local business pages show only approved active sponsor placements and never expose child profiles, parent contacts, private media, billing state, or redemption proof. | Live fee tables, sponsor contract workflow, discount redemption proof, Stripe checkout/settlement webhooks, sponsor email delivery, and hosted browser proof for the new surfaces. |
| Community safety follow-ons | `/parent`, `/team-portal`, `/admin`, `/admin/media-review`, `/admin/safety-weather` | One-Tap Volunteer Marketplace, Equipment Exchange, Weather + Safety Decision Assistant, Sponsor-Safe Media Gallery, and Family Availability Intelligence are implemented as deterministic read models over existing snack, volunteer, schedule, RSVP, media, sponsor, and weather rows. Parent-facing jobs reuse authenticated snack/volunteer claim APIs where a real slot exists. Equipment and sponsor-safe media stay moderated and hide parent contacts, child profiles, private media metadata, and unapproved listings. Availability signals are aggregate team counts only, not parent rankings. | Durable equipment-listing persistence, hosted browser proof, richer admin approval actions, provider-backed weather refresh proof, and notification/reminder send workers. |
| Mobile-first UI | All routes | Responsive CSS stacks grids/navigation for small viewports. Parent, coach, and admin homes now use mobile-first Season Certainty cards, stable status badges, stacked action rows, responsive team-status rows, and disclosure-compressed detail sections before dense workflow content. PWA/native usage metrics are recorded so Expo stays evidence-gated and can reuse shared domain models if justified. | Browser/device mobile QA pass, touch targets across deeper workflows, offline states, and app-store requirements if Expo is added. |
| PWA installation | All routes, `/offline`, `public/manifest.webmanifest`, `public/sw.js`, `/api/mobile-usage-events` | Manifest, production-only service worker registration, install prompt UX, standalone build asset copy, explicit offline fallback, and install/standalone usage measurement exist. Local development unregisters stale localhost workers so dev/HMR does not blink through cached app shells. The production worker no longer cache-first serves private route HTML; navigation requests go to network first and fall back to `/offline` only when offline. Smoke tests assert `/offline`, network-first navigation fallback, and usage recorder wiring. | Richer install QA across Chrome/Edge mobile and desktop, plus hosted stale-brand/cache proof after deployment. |
| Dark mode | All routes | System `prefers-color-scheme: dark` CSS variables are present. | Theme QA for all routes, manual toggle if desired, and per-team brand contrast checks. |
| Communication Room and Branded Team Chat | `/parent/messages`, `/coach/messages`, `/team-chat`, `/api/team-chat/*`, `/api/notifications/acknowledge` | The parent route renders authority-separated Critical, Updates, and Conversation lanes over server-scoped team chat and recipient-scoped notification evidence. Unapproved drafts are withheld, fallback conversation is read-only, and receipt acknowledgment remains an audited recipient action. Signed-in Chromium comparison passes at 390, 768, and 1440 pixels. Isolated Supabase QA proof passed real-session RLS, multi-child/team isolation, reply persistence, acknowledgment and audit readback, and zero-provider-send boundaries. Coach and compatibility routes retain the branded clubhouse UI, Supabase persistence, read receipts, Realtime wiring, and moderation controls. | Repeat production-hosted proof only after explicit deployment and migration promotion; add durable message version, correction, withdrawal, and supersession records; schedule reporting and retention jobs. |
| Parent Replay | Coach/family routes plus practice-run and draft/approve/publish APIs | Plan save, practice start, completion observations, Replay draft, approval, and publication are independent. A completed same-team practice receipt may be linked once and becomes explicit source evidence. Publication creates notification drafts and never sends automatically. | Apply migration `0024`/RLS and refresh hosted coach/family proof; provider delivery remains a separate approved operation. |
| AI-generated learning plans | `/team-portal`, `/coach/parent-replay`, `/api/coach/ai-workspace` | Parent Replay remains deterministic local guidance. AI Coach Workspace can request OpenAI Responses API rewrites through an authenticated server route for assigned coaches/admins only when provider env is configured. Requests use `store: false`, local privacy filters, source evidence, approved-media filtering, and review-only output. Hosted proof passed with `QA_PROOF_BASE_URL=https://www.leaguepilot.us npm run qa:ai-coach-proof`, capturing `output/playwright/ai-coach-provider-rewrite-qa-session-live.png`. Safety evals cover cross-team data, private contacts, hidden media/messages, unsupported provider-send/publish claims, and unsourced private/external claims. | Preview env configuration remains out of launch scope until a real preview branch is chosen; continued approval/audit gates remain required before any family-facing use. |
| Assistive suggestions | `/admin`, `/coach`, `/parent` | Deterministic admin, coach, and parent suggestion cards summarize existing scoped records. They recommend, draft, and prioritize only; sensitive actions still require human clicks and existing private APIs. | Optional AI provider expansion only after evals for privacy, team scope, provider-send safety, and hallucinated records. |
| Reporting and archive | `/api/admin/exports`, `docs/archive-readiness-checklist.md` | Active org admins can generate audited CSV exports for roster, contacts, schedule, RSVP, snacks, volunteers, sponsors, and notifications. Related reads and profile lookups are narrowed to selected organization IDs, with local cross-tenant regression coverage. Archive checklist requires non-chat season preservation and chat-retention deletion proof. | Hosted RLS/admin export proof and hosted archive smoke proof after migrations are applied and a real season close workflow is run. |

## State Legend

- Local scaffold: works in the current app with typed seed data and reducer state only.
- Provider boundary: record/draft exists, but no external send or provider call occurs.
- Production gap: needed before real families, teams, money, or provider delivery depend on the capability.
