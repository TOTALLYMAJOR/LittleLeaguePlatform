import { CoachRosterSurface } from "../_surfaces";

export const dynamic = "force-dynamic";

export default async function CoachRosterPage() {
  return <CoachRosterSurface />;
}

export const metadata = {
  title: "Roster"
};
