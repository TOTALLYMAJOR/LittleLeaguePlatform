import { ParentMessagesSurface } from "../_surfaces";

export const dynamic = "force-dynamic";

export default async function ParentMessagesPage() {
  return <ParentMessagesSurface />;
}

export const metadata = {
  title: "Messages"
};
