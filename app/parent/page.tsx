import { ParentHomeSurface } from "./_surfaces";

export const dynamic = "force-dynamic";

export default async function ParentPage() {
  return <ParentHomeSurface />;
}

export const metadata = {
  title: "Family Home"
};
