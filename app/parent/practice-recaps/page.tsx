import { ParentPracticeRecapsSurface } from "../_surfaces";

export const dynamic = "force-dynamic";

export default async function ParentPracticeRecapsPage() {
  return <ParentPracticeRecapsSurface />;
}

export const metadata = {
  title: "Parent Replay"
};
